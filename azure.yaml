# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: "{{PROJECT_NAME}}"
metadata:
  template: ai-sdlc-blueprint

services:
  app:
    project: ./src
    host: containerapp
    language: ts
    docker:
      path: ./Dockerfile

infra:
  provider: bicep
  path: ./infra
